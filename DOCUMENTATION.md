# HPM Intranet — Developer Documentation

Master index of the developer / implementation documentation for the **HPM Intranet** (formerly "Hotel HR App") — a cloud HR platform for Special Tours Prague (STP) / Hotel Property Management (HPM).

Looking for how to *use* the app? User-facing documentation lives in the app itself — the **Nápověda** page (`/napoveda`), which renders one topic per permission from the guided tour. The cross-cutting behavioural rules are in **[`docs/business-rules.md`](docs/business-rules.md)**, and the Czech Recepce / Návody / Průvodce chapters are in the [README](README.md). The per-role `manuals/` directory was retired 2026-07-20; do not re-create it.

## Technology stack

| Layer | Technology |
|---|---|
| Frontend | React + TypeScript (Vite) — `frontend/` |
| Backend | Firebase Cloud Functions (Express) — `functions/` |
| Database | Firestore (NoSQL) |
| Auth | Firebase Auth with custom role claims |
| File storage | Firebase Storage |
| Contract generation | TipTap WYSIWYG + server-side Puppeteer PDF |
| Encryption | AES-256-GCM in Cloud Functions |

Firebase project: `hotel-hr-app-75581` (production), `hote-hr-app-staging` (staging).

## Topics

| Document | Covers |
|---|---|
| [Provozní pravidla (business rules)](docs/business-rules.md) | **Czech, cross-cutting.** The non-obvious behavioural rules a user cannot infer from the UI: numeric limits, eligibility conditions, what an approval triggers, what carries forward, what is irreversible, and what the app merely advises. Every rule carries a code reference and an enforcement marker (server / frontend-only / automatic), because two previously-documented rules turned out to have silently stopped being true. Created 2026-07-20 when the per-role `manuals/` were retired; it is the home for this class of knowledge, which by design fits neither the tour steps (one rule per button) nor the topic files below (per-module, English, implementation-oriented). |
| [Data Model & Build Phases](docs/data-model.md) | Firestore top-level collections, sub-collections, denormalized fields, build-phase status, and the read-time (never-backfilled) live employee-name resolution pattern used by every surface that once snapshotted a name at write time. |
| [Authentication, Roles & Permissions](docs/auth-and-permissions.md) | Login + password reset, the configurable permission model (catalogue + editable user types + per-user grants/revokes), `requirePermission` enforcement, the admin type/permission UI, per-role menu order, the phone-only `mobilePermission` gating pattern, the `sharedTerminal` user-type flag (Recepce write attribution), the **v4.11.1** `noSelfLogout` user-type flag + `system.logout.authorize` permission (password-authorized logout release for shared terminals — a UX guard, not a security boundary; live-read + fail-open invariants), and the **v4.12.0** `roleTypeFromUserDoc()` standing rule for reading another user's type (legacy pre-RBAC-overhaul docs carry only `role`, not `roleType` — bit `getManagementEmployeeIds` specifically). |
| [Employees](docs/employees.md) | Employee module, session-based employment history (incl. parental-leave RODIČOVSKÁ rows with optional end date, and the **v4.11.0** one-in-play-at-a-time rule enforced server-side on both create and edit), concurrent contracts (simplified model — latest active contract wins), PPP part-time `hoursPerWeek` field, minimum-wage non-blocking warning, non-+420 phone format modal, `Můj profil` self-service (signed-contract-only history filter + self contract download), self document-expiry alerts + Můj profil badge, employee-detail tab restructure (ad-hoc documents interleaved into Další dokumenty, Prohlášení poplatníka moved to the history toolbar), the blank print-and-fill Dotazník endpoint (**v4.11.0**, ungated beyond page access, no audit entry), the non-blocking weekend/public-holiday signing-date advisory shared with Contracts (**v4.11.0**, `lib/workingDays.ts`), the `displayName` alias field (`employeeDisplayName`/`employeeSurnameFirst`) vs. the legal name kept on contracts/tax forms/CSV/pickers, the **vacation-hour ledger (v4.9.0)** — per-employee/per-year nárok/čerpáno/zůstatek in hours, fed from an AVENSIO seed, the payroll-lock cascade, and manual edits gated by `employees.vacationBalance.manage`, plus its **v4.12.1** read-only self-service mirror on Můj profil (`GET /me/employee/vacation-ledger`, new `vacation.balance.view.self` permission — a real server-side gate, no self PATCH, shared `readLedger()` service and shared `VacationLedgerSection` component via a new `basePath` prop) — and the **v4.12.0** "Hromadné generování" bulk-document button (full write-up in Contracts & Templates). |
| [Shifts (Shift Planner)](docs/shifts.md) | Shift-expression parser, monthly grid, MOD badges (letter → `employeeId` in `modPersons`, resolved through the shared `lib/modPersons.ts` helper — no employee names in source, carried across months by `copy-employees`), structured shift-change-request picker + auto-apply on approval, double-click X on open plans, X-limits (incl. server-side self-service enforcement), plan lifecycle, numeric-cell shift-type tagging (per-type tally), optimistic-concurrency cell guard, and (**v4.11.0**) the `GET /shifts/plans-upcoming` route that announces a still-unopened plan's scheduled opening time to self-service viewers without exposing the roster, plus the absolute deadline date/time now shown to users who can't edit deadlines. |
| [Vacation (Dovolená)](docs/vacation.md) | Request/approval workflow, pendingEdit pattern, shift-collision handling. (For the separate per-employee vacation-**hour ledger** — nárok/čerpáno/zůstatek — see [Employees](docs/employees.md).) |
| [Recepce (Reception)](docs/recepce.md) | Permission-driven per-hotel hub (Ambiance/Superior/Amigo & Alqush/Ankora); per-user default hotel (`recepceDefaultHotel`, self-service + admin write paths); Předávací protokol (cash/trezor counting, sm/sm-trezor/wata, virtual dual-signature handover, freeze-on-sign, element-level history + undo/redo, coalesced typing edits, single "created" floor entry); Walkiny and Taxi (visible-range gating, global taxi ceník, manager Provize total); Lobby bar (Ambiance only — server-side snapshotted price/provision/doSpolecne per sale, per-currency item ceník + provision rates); Terminál (Amigo & Alqush only — enum transaction types + optional note, manage-only "Předáno" settlement flag); shared-terminal write attribution (`sharedTerminal` roleType flag → `resolveRecepceActor`/`resolveOnDutyActor`, `viaUid`/`viaEmail`); recepce retention sweep; mobile-only `recepce.mobile.view` gating pattern; and an internal low-profile summary page (`recepce.summary.view` — see docs/recepce.md). |
| [Contracts & Templates](docs/contracts.md) | Contract types, TipTap template editor, server-side PDF generation, template variables (incl. PPP `{{hoursPerWeek}}`, `{{newHoursPerWeek}}` / `{{isDodatekHodiny}}`, and `{{isMale}}`), editable variable overview at generation time, per-template custom variables (`{{var1}}`..`{{var10}}`, label+type+optional **default value (v4.8.2)** configured per template, never persisted, plus a **`condition` slot type (v4.10.0)** whose Ano/Ne value is computed from a comparison of two comparable built-ins/literals rather than typed in — replaces the removed `{{isHalfTime}}`/`{{salaryChangeVerb}}` permanent variables), nested `{{#if}}`/`{{#unless}}` conditional blocks (v4.8.0 block-parser rewrite), in-editor + byte-accurate PDF template preview (v4.8.0) with **editable condition-operand fields (v4.10.0)** so a derived condition can be driven live in the náhled, **one-step ad-hoc document generation (v4.8.2 — replaces the old row-first empty-PDF-row flow for new documents)**, **probation-aware termination-template selection + DPP fix (v4.8.2)**, signed-contract "Smlouva + prohlášení" split with client-side scan compression, de-duplicated boolean variables (single-key + `{{#unless}}`) and the deploy-order trap when changing the permanent variable catalogue. **v4.12.0** adds: client-orchestrated **bulk standalone-document generation** ("Hromadné generování" — deliberately N single-document calls, no bulk endpoint, for the Puppeteer 60s budget/per-id access scoping/body-size reasons documented in the doc) plus its client-side `pdf-lib` print-merge and the display-only `contracts.generate.bulk` permission; `resolveStandaloneEmployment()` so standalone documents resolve employment tokens from the employee's running contract (and deliberately inverts the concurrent-contract tie-break used for `current*` root fields); and confining the "Smlouva + prohlášení" split-upload option to employment-history rows only (ad-hoc/standalone documents get a plain "Nahrát podepsaný dokument" upload). |
| [Other Features & UI](docs/other-features-and-ui.md) | Dashboard (`Přehled`), audit log, `Upozornění` hub (incl. the v4.8.2 probation-alert end-date fix — now the last in-probation day, matching the termination-template rule), dark mode, mobile responsiveness (`BottomNav`, breakpoints, card-on-mobile, incl. the v4.11.0 dark-mode footer-divider fix and the "Více" sheet's deliberate backdrop-click-to-dismiss exception), shared UI components, companies, and the Settings **Seznamy** tab (v4.8.1 consolidation of the former Společnosti/Oddělení/Pracovní pozice/Vzdělání tabs into one tab of collapsible sections). |
| [Dokumenty](docs/dokumenty.md) | **v4.15.0.** Standalone TipTap template editor for non-contract printable documents (`documentTemplates/{id}`), beside Šablony smluv. Custom variables (`{{var1}}..{{var10}}`) only — no employee binding, so no `condition` slot type and no `fixedVar` defaults; nothing is persisted from a render (`POST /render-pdf` reuses `services/pdfRenderer.ts` gated on `nav.dokumenty.view`, not `contracts.generate`); the four hard-coded sections (Ambiance/Superior/Amigo & Alqush/Ankora) and why they can't be admin-created, the 404-not-403 existence-hiding rule, and the unknown-section-is-restricted rule; the per-user `dokumentyDefaultSection` sort-order preference and its read-straight-from-useAuth landmine; the new shared `"list"`/"Seznam" custom-variable type (`CustomVarDef.options`); and the `lib/editor/extensions.ts` TipTap-extension extraction shared with `ContractTemplatesPage.tsx` (config dialog deliberately not shared). |
| [Tabulky (Směnárna + ČNB)](docs/tabulky.md) | **v4.14.0.** Standalone top-level tabbed page for calculation tables belonging to no single hotel. Tab registry in `lib/tabulky.ts` (gating by omission, mirroring `lib/hotels.ts`); `nav.tabulky.view` + `tabulky.smenarna.view`; `GET /api/exchange/rates` as a separately-gated reader of the same `settings/sm` rates the Recepce sm row owns (and the positional €/$/£ coupling that implies); the Směnárna calculator — note swap funded from exchange proceeds via the spreadsheet's restored H column, bounded-greedy denomination decomposition with the 5000 note capped at what was actually received and allocated largest-first, and the three warning rules; and Historie — shared `smenarnaSnapshots` saved/recalled explicitly (never auto-restored), audit-logged, swept after 6 months by the separate `sweepSmenarnaSnapshots` job. |
| [Guides (Návody)](docs/guides.md) | Permission-gated `/navody` reference-material page: `guides` Firestore schema (free-form `tags[]`, no category collection), server-derived tag vocabulary + `normalizeTags`, client-side diacritics-insensitive search + AND tag-chip filtering, `guides/{id}.pdf` Storage convention (Cloud-Function-only access), base64 upload transport + 7 MB ceiling, fetch→blob→iframe PDF viewer with a phone fallback, http(s)-only link validation, and the `nav.guides.view` / `guides.manage` permission pair. |
| [Onboarding Tour & Nápověda](docs/onboarding-and-help.md) | Guided first-login tour, permission/exclude gating, demo-route mock architecture (incl. the `/zamestnanci/tour-demo` Seznamy-catalogue interception, v4.8.1), tour persistence, per-step `deltaTitle`/`deltaBody` "this moved" copy for the "Co je nového" delta tour, and the Nápověda Help page (incl. its v4.8.1 clickable steps that jump the guided-tour overlay to a specific step via `startTour(atStep)`). |
| [Deployment & Environments](docs/deployment.md) | Project aliases, deploy scripts, Secret Manager, Firestore indexes, manual triggers, test clock. |

## Local-only references (not committed to git)

These live in the working tree but are gitignored:

- `payroll.md` — the canonical payroll-computation reference (calculation rules, cascades, `payrollPeriods/*` schema, locking/recalc, DPP/FAKTURA, Multisport, notes). All payroll detail lives here, intentionally not in the docs above.
- `CLAUDE.md` — working guidelines and project conventions for the AI assistant.
- `TODO.md` / `TODO_BIG.md` — the prioritised backlog.

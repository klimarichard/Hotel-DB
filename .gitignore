# Seed scripts and snapshots (contain sensitive data — employee IDs, credentials, local paths)
scripts/

# Specification document
HR_App_Specification.docx

# Legacy — seed CSVs are now under scripts/seeds/ (already excluded by scripts/)

# Dependencies
node_modules/

# Environment variables
.env
.env.*
!.env.example

# Build outputs
dist/
build/
functions/lib/

# Firebase
.firebase/
.firebaserc.local

# Claude Code local settings (machine-specific paths)
.claude/settings.local.json

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

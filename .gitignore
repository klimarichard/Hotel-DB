# Seed scripts and snapshots (contain sensitive data — employee IDs, credentials, local paths)
scripts/

# Specification document
HR_App_Specification.docx

# Local task list (personal, not tracked in git — see TODO.md)
TODO.md

# Payroll implementation notes — kept local
payroll.md

# Claude working instructions — kept local
CLAUDE.md

# Staging→prod promotion runbook — kept local
DEPLOY_RUNBOOK.md

# Local data exports & source documents (sensitive — employee/payroll data; not tracked)
DTB.xlsx
MZDY.xlsx
OTH-L-CMYK-Blend.pdf
excels/
doc_templates/

# Local working notes & screenshots (not tracked)
TODO_change_log.md
tour_notes.md
PERMISSIONS_LIST.md
screenshots/

# Editor / IDE settings (machine-specific)
.vscode/

# Legacy — seed CSVs are now under scripts/seeds/ (already excluded by scripts/)

# Dependencies
node_modules/

# Environment variables
.env
.env.*
!.env.example

# Build outputs
dist/
build/
functions/lib/

# Firebase
.firebase/
.firebaserc.local

# Emulator / debug logs (firebase-debug.log, firestore-debug.log, emulator.log, …)
*.log

# Claude Code local settings (machine-specific paths)
.claude/settings.local.json

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db
